<Page x:Class="Login.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Login.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Авторизация">
    <Grid Background="#ffffff">
        <StackPanel Margin="20" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Label Content="ВХОД" HorizontalContentAlignment="Center"/>
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition MinWidth="400"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Label Content="Логин:" Grid.Column="0" Grid.Row="0" Margin="5"/>
                <Label Content="Пароль:" Grid.Column="0" Grid.Row="1" Margin="5"/>
                <TextBox x:Name="txbLogin" Grid.Column="1" Grid.Row="0" Margin="5"/>
                <PasswordBox x:Name="psbPassword" Grid.Column="1" Grid.Row="1" Margin="5"/>
            </Grid>
            <Button Name="btnPassword" Content="Войти" Click="btnPassword_Click" Margin="5"/>
            <Button Name="btnRegistration" Content="Зарегестрироваться" Click="btnRegistration_Click" Margin="5"/>
            <Label Name="Timer" HorizontalContentAlignment="Center" Visibility="Visible" Content=""/>
        </StackPanel>
    </Grid>
</Page>
